/ Online C compiler to run C program online
#include <stdio.h>

void main() {
    float l,w,area,perimeter;
    printf("enter length: ");
    scanf("%f",&l);
    printf("enter width: ");
    scanf("%f",&w);
    area= l*w;
    perimeter=(2*(l+w));
    printf("area = %.3f\n",area);
    printf("perimeter = %.4f",perimeter);
    
    
}
